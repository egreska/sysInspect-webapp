/* Replaced at container start by docker-entrypoint.sh when using Docker.
   For local dev, leave empty — the app uses Vite .env (import.meta.env). */
window.__CLOUDKIT_RUNTIME_CONFIG__ = {
  containerId: '',
  apiToken: '',
  environment: '',
};
